# TODO

- [ ] **Metadata Update**: Update the `mLRS_metadata.py` (or JSON equivalent on GitHub) to include `inav_passthrough` in the `flash_method` list for relevant receivers (ESP32, STM32 UART-capable). This will ensure the option appears natively without the UI force-injection hack.
